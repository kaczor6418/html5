import { crateConfig, platformConfig, truckConfig } from './config.js';

console.log('Truck config:', truckConfig);
console.log('Platform config:', platformConfig);
console.log('Crate config:', crateConfig);